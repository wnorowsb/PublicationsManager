version: '2'
services:
    web:
        build: .
        ports:
            - "5000:5000"
        container_name: 'web'
        volumes:
            - .:/var/www/app
        depends_on:
            - redis
    redis:
        image: redis
    
